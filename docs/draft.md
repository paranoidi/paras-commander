
# Subshell
zsh users need setopt HIST_IGNORE_SPACE for injected lines to skip history, and bash ≥5.1 array-style PROMPT_COMMAND uses the string-append form. Worth a quick manual smoke in your terminal: Ctrl+O, cd through a symlink, toggle out — and Alt+Enter on a selection in fish.

