# Paras Commander

The `pc` binary (build with `go build ./cmd/pc` from the repo root) is a Linux terminal twin-panel file manager inspired by Midnight Commander.

## Critical

NOTE: This application is not yet stable or usable. If you issue copy or move commands expect data loss.


## TODO

- Consider removing regression tests (implementation artifacts)
