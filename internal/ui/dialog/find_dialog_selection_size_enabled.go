package dialog

// FindDialogSelectionSizeEnabled gates marked-selection count/size on the find dialog separator
// and background directory size scans (reconcileFindDialogSelectionSizeScans).
// Temporarily false due to performance regression with large mark sets.
const FindDialogSelectionSizeEnabled = false
